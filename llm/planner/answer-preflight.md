Return a direct answer when chat context, media context, or stable knowledge is
enough. Request research only when freshness, entity identity, a disputed fact,
or a linked source materially affects correctness. Resolve references from the
nearby chat, then choose one checkable focus claim and one neutral query.

Return only JSON matching OUTPUT_CONTRACT.
